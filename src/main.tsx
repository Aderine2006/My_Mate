import React from 'react';
import ReactDOM from 'react-dom/client';
import MYMate from './mymate-tracker';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <MYMate />
  </React.StrictMode>,
);

